import React from 'react'

type Props = {
    option: string
    word?: string
}

const SearchResult = ({option, word}: Props) => {
    // console.log(option)
    // console.log(word)
  return (
    <div>SearchResult</div>
  )
}

export default SearchResult